# ═══════════════════════════════════════════════════════
#   J.A.R.V.I.S. Dependencies
#   pip install -r requirements.txt
# ═══════════════════════════════════════════════════════

# ── Core ──────────────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0
huggingface-hub>=0.23.0     # Model downloads from HuggingFace
tqdm>=4.66.0                # Download progress bars

# ── AI Engine (local, no API key) ─────────────────────
# CPU install:
llama-cpp-python>=0.2.90
# GPU (CUDA 12.1) — comment out the line above and use this instead:
# --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# llama-cpp-python>=0.2.90

# ── AI Engine (cloud options) ─────────────────────────
groq>=0.9.0                 # Groq: free Llama 3.1 70B, 300+ tok/sec

# openai>=1.30.0            # Uncomment for OpenAI via OpenRouter
# anthropic>=0.28.0         # Uncomment for Anthropic direct

# ── Speech Recognition ────────────────────────────────
faster-whisper>=1.0.0       # Local offline STT
sounddevice>=0.4.6
numpy>=1.24.0

# ── Text to Speech ────────────────────────────────────
edge-tts>=6.1.12            # Free neural TTS (en-GB-RyanNeural)
pygame>=2.5.0               # Audio playback

# ── Wake Word ─────────────────────────────────────────
# openwakeword>=0.6.0       # Uncomment for hands-free "Hey Jarvis"
keyboard>=0.13.5            # Ctrl+Shift+J hotkey fallback

# ── Internet Skills ───────────────────────────────────
duckduckgo-search>=6.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Knowledge Base (RAG) ──────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ── System Skills ─────────────────────────────────────
psutil>=5.9.0

# ── Terminal UI ───────────────────────────────────────
rich>=13.7.0

# ── Document Ingestion (optional) ─────────────────────
# pdfplumber>=0.10.0
# python-docx>=1.1.0

# ── Fine-tuning (optional, requires GPU) ──────────────
# unsloth[colab-new]
# trl>=0.8.0
# peft>=0.10.0
# accelerate>=0.29.0
# bitsandbytes>=0.43.0

# ── Windows .exe packaging ────────────────────────────
# pyinstaller>=6.0.0        # Installed automatically by build_windows.bat
